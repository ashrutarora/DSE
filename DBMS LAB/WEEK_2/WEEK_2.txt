2.1
ALTER TABLE EMP
ADD(REPORTS_TO NUMBER(3) CONSTRAINT FK_REPORTS_TO_EMPCODE REFERENCES EMP);

2.2
ALTER TABLE SKILL
ADD CONSTRAINT STRTSWITH_S
CHECK( SKILLID LIKE ('S%')
);

2.3
ALTER TABLE EMP_SKILL
ADD CONSTRAINT PK_EMPNO_SKILLID
PRIMARY KEY(EMPNO, SKILLID);

2.4
ALTER TABLE PRJ_DETAILS
ADD CONSTRAINT UNQ_PRJ_NAME
UNIQUE(PRJ_NAME);

2.5
ALTER TABLE CLIENT
ADD (EMAIL VARCHAR2(30) CONSTRAINT VALID_EMAIL CHECK (EMAIL LIKE('%@%.___'))
);

2.6
ALTER TABLE EMP
ADD CONSTRAINT UNQ_PHONE
UNIQUE(PHONE);

2.7
ALTER TABLE EMP
ADD CONSTRAINT UNQ_EMAIL
UNIQUE(EMAIL);

2.8
ALTER TABLE MEDICAL_POLICY
DROP PRIMARY KEY;

ALTER TABLE MEDICAL_POLICY
ADD CONSTRAINT PK_MEDPOLICY
PRIMARY KEY(EMPNO, POLICYNO, POLICYDATE);

2.9
ALTER TABLE MEDICAL_POLICY
DROP CONSTRAINT CANT_BE_ZERO;

ALTER TABLE MEDICAL_POLICY
ADD CONSTRAINT PREM_GRTR_0
CHECK (PREMIUM > 1000);
